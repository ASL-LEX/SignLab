query getDatasets {
  getDatasets {
    id,
    name,
    description,
    creator {
      id,
      name,
      email,
      username,
      roles
    },
  }
}

query projectExists($name: String!) {
  projectExists(name: $name)
}
