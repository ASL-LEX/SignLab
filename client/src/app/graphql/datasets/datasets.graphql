query getDatasets {
  getDatasets {
    id,
    name,
    description,
    creator {
      id,
      name,
      email,
      username,
      roles
    },
  }
}

query datasetExists($name: String!) {
  datasetExists(name: $name)
}
